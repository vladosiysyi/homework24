Skystore — это Django-проект, представляющий каталог цифровых продуктов.

## Установка
```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python manage.py runserver
```

## Для запуска сервера 
```bash
python manage.py runserver
```